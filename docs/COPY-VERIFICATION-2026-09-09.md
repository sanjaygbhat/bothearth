# Website copy verification — 9 September 2026

Reviewed all 12 configured website pages, their metadata, the README, quickstart and the app’s About text against the implementation, recorded test evidence and live service availability. The starting public revision was [`8be3b94`](https://github.com/sanjaygbhat/bothearth/commit/8be3b943a8b6ad1095b6da63631744b80331f406). This is an internal copy review, not an independent security audit or legal opinion.

| Statement | Evidence checked | Scope or correction |
|---|---|---|
| One business bot free; commercial outputs allowed | [Business permission](../COMMERCIAL.md), [licence](../LICENSE) | Registration is not required. Standard free and paid grants exclude commercial replication or hosting of the underlying service. This verifies the published terms, not enforceability in every jurisdiction. |
| US$49 introductory price; US$99 reference price | Published offer and price arithmetic: `(99 − 49) / 99 = 50.5%` | US$99 is identified as the **planned standard price**, not an established prior selling price. Payment collection is not open. There is no announced expiry date. |
| Work-domain certificates and private contact | [Site builder](../scripts/build-site.mjs), [enterprise service](../enterprise/server.mjs), [contact service](../enterprise/contact.mjs), [website tests](../tests/unit/website/) | Live pages were fetched: certificate claims unavailable; contact fieldset disabled. Copy now distinguishes the implemented service from what visitors can use. No email was sent during this check. |
| One certificate shared by a domain | Enterprise claim insert uses a unique domain and `ON CONFLICT(domain) DO NOTHING`; existing enterprise tests exercise repeat and subdomain claims | Domain grouping does not establish legal authority or detect every organisation owning several domains. The one-bot legal allowance applies across those domains. |
| Real inbox test took 51 min 6 s across two runs | Original task database opened read-only: 1,817,059 ms failed initial run plus 1,248,856 ms completed continuation | Rounded combined duration verified. This is two runs, not one uninterrupted task or a 12-hour test. See the [email test record](EMAIL-ORGANISATION-TEST-2026-09-08.md). |
| Inbox and completion images have private details blurred | Both public source image hashes rechecked against retained redaction receipts | “Unretouched” removed from the FAQ. Mailbox outcome counts are attributed to the task report; private raw evidence is not published. |
| Task cost is an estimate | Default `DEFAULT_MCP_TOOL_CALL_PROXY_USD = 0.01` in [daemon](../src/daemon/server.ts); [task UI](../src/ui/task.ts); [usage UI](../src/ui/usage.ts) | Native tool calls default to an estimated cent each. This is not measured provider spend. Removed instructions for the deleted Settings budget input; internal safety limits still exist. |
| Node.js 22.18+ and source installation | [Package engines and commands](../package.json), [CLI initialization](../src/cli/init.ts), build workflow | Desktop Linux can use Secret Service. A headless VM needs an available vault provider; pressing Enter is not a universal unattended setup solution. |
| Paired sessions expire after seven inactive days or 30 days total | [Authentication constants](../src/daemon/auth.ts), [session storage](../src/daemon/store.ts) | Bootstrap links are one-use and expire after ten minutes. These are app sessions, separate from enterprise sessions. |
| Scheduled tasks require a standalone provider | Routine launch in [daemon](../src/daemon/server.ts) rejects missing standalone agent-loop configuration | Native Codex/Claude Code connections are not advertised as the scheduler path. |
| Operator hosting preserves primary storage control | [Security model](../SECURITY.md), [privacy notice](../PRIVACY.md), daemon and container configuration | Remote models still receive model-visible context. Host model CLIs retain their own permissions. No “all data stays offline” or universal isolation claim. |
| Human control blocks model capture | Takeover lease/restart regressions and the [real VM capture check](REMOTE-VM-TRIAL-2026-09-09.md#restart-and-control-evidence) | Applies during acknowledged control. VM browser recovery and graphical input were not proven seamless. |
| Security fixes and test counts | Re-fetched successful [CI receipt](https://github.com/sanjaygbhat/bothearth/actions/runs/34317006444) and [container receipt](https://github.com/sanjaygbhat/bothearth/actions/runs/34317052358), both at `ea71e4d` | Historical scoped results, not an audit of every later revision. Clarified that 1,223 unit passes were the local follow-up suite; Linux root checks ran separately on the VM and CI. |
| Current build has CI evidence | Re-fetched successful [CI at 8be3b94](https://github.com/sanjaygbhat/bothearth/actions/runs/34334006852): Node 22, Node 24 and macOS jobs | This is build/test evidence, not proof that arbitrary model outputs are correct. |

The review date now comes from the site configuration. Known stale wording is checked across every configured page by `npm run claim-scan`; availability branches are checked by `npm run site:test`. These guards detect known regressions. They do not establish the truth of arbitrary new marketing claims: new capabilities still need implementation and test evidence.

Remaining launch limits: online certificate claims, contact submission and payment collection were not open when verified. No claim is made that this copy update activates them. The free published business permission and source installation are already available.
